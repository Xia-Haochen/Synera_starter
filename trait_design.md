# 羁绊设计说明

**属性光环类：** 

**1.** 战士：`trait_count` 到达 2 时触发 1 级羁绊，所有战士血量变为未触发羁绊时的 1.5 倍；`trait_count` 到达 4 时触发 2 级羁绊，所有战士血量变为未触发羁绊时的 2 倍。

**2.** 弓手：`trait_count` 到达 3 时触发羁绊，所有弓手攻击力变为未触发羁绊时的 1.5 倍。



**机制改变类：** 

**1.** 法师：`trait_count` 到达 3 时触发羁绊，释放技能时所有被攻击目标眩晕一回合。